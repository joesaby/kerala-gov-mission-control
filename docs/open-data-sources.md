# Open data sources — research snapshot

This research note has been merged into the canonical inventory at
[`docs/data/`](./data/). Per-domain entries, the sync plan, and the typed
registry at [`data/sources.ts`](../data/sources.ts) are now authoritative.

The original 2026-05-18 research note (prose summary, GREEN/AMBER/RED bucket
table, "couldn't-verify" appendix) is preserved in git history — see
`git log -p -- docs/open-data-sources.md`.
